<!-- ---
import { type CollectionEntry, getCollection } from "astro:content";
import Layout from "../layouts/Layout.astro";
import Title from "../components/Title.astro";

// export async function getStaticPaths() {
//   const posts = await getCollection("blog");
//   return posts.map((post) => ({
//     params: { slug: post.slug },
//     props: post,
//   }));
// }

export async function getStaticPaths() {
  const tops = await getCollection("top");
  return tops.map((top) => ({
    params: { path: top.slug },
    props: top,
  }));
}
type Props = CollectionEntry<"top">;

const top = Astro.props;

const { Content } = await top.render();
// title
// post.data.frontmatter
// console.log(top.data.title);
---

<Layout {...top.data}>
  <Title text={top.data.title} variant="display" element="h1" class="py-4" />

  <Content class="py-3" />
</Layout> -->
